[general]
name=QGIS MCP HTTP
description=Expose QGIS as a localhost Streamable HTTP MCP server for Codex, Claude, and other MCP clients.
version=0.1.0
qgisMinimumVersion=3.22
author=Aaron Mineen
email=amineen64@hotmail.com
category=Plugins
tags=mcp,llm,ai,automation,pyqgis
homepage=https://github.com/amineen/qgis-mcp-http#readme
repository=https://github.com/amineen/qgis-mcp-http
tracker=https://github.com/amineen/qgis-mcp-http/issues
experimental=True
license=GPL-2.0-only

[about]
about=QGIS MCP HTTP starts a localhost-only Streamable HTTP MCP endpoint directly inside QGIS. It is a GPL-2.0 derivative, published with permission, of the command handler implementation from jjsantos01/qgis_mcp. It exposes those QGIS tools at /mcp so clients can inspect projects, manage layers, run processing algorithms, render maps, and optionally execute PyQGIS code without a separate local stdio server checkout.
